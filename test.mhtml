<p>输入 $a, b$，求 $C_a^b$ 的值。</p>
<p>注意结果可能很大，需要使用高精度计算。</p>
<h4>输入格式</h4>
<p>共一行，包含两个整数 $a$ 和 $b$。</p>
<h4>输出格式</h4>
<p>共一行，输出 $C_a^b$ 的值。</p>
<h4>数据范围</h4>
<p>$1 \le b \le a \le 5000$</p>
<h4>输入样例：</h4>
<pre><code>5 3
</code></pre>

<h4>输出样例：</h4>
<pre><code>10
</code></pre>